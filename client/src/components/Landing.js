import React from 'react';

const Landing = () => {
    return (
        <div className="center-align">
            <h3>checkMyToken!</h3>
            Waiting for Doctor is no longer required
        </div>
    );
};

export default Landing;